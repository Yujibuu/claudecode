# 本日の報告用スライド構成案(2026/9/11)

> 前日(9/10)のスライド構成を踏襲。「①今日のゴールと結果」「②クラウドでサーバーを立てる流れ × 検証状況」「③明日の学習予定」の3段構成。

---

## タイトル案
「IBM Cloudアサインに向けた学習 進捗報告(9/11)」

## ① 今日のゴールと結果

| 今日のゴール | 結果 |
|---|---|
| 昨日❌だった①VPC・②サブネット・③SGを、Google Cloud Shell上のDockerで疑似体験する | ✅ ②③は実機体験まで到達。①は概念補足止まり(Dockerに対応機能なし) |
| 「サーバーとは何か」を、何も見ずに自分の言葉で説明できるようにする | ✅ 達成(上長からの指摘をきっかけに定義を掘り下げ、自力で説明できるレベルに到達) |

## ② 本日の実績(詳細)

**VPC/サブネット/SGの疑似体験(Dockerカスタムネットワーク)**
- `docker network create --subnet=...` で2つの独立したサブネット(`public-subnet` 10.0.1.0/24, `private-subnet` 10.0.2.0/24)を作成
- 別サブネットに所属するコンテナ間は、デフォルトで通信不可(100% packet loss)であることを確認 → **VPC/サブネットの隔離**を体験
- `docker network connect` で明示的に接続すると通信可能になることを確認(ネットワーク的な経路の追加、に相当)
- 同一サブネット内の2台で`iptables`を使い、**経路は変えずにポート単位で通信をブロック/許可**できることを実演 → これが**SG本来の動き**(経路の有無ではなく、許可判定)であることを体験

**「サーバーとは何か」の言語化**
- 上長に問われて答えられなかったことをきっかけに、今日の実体験(`httpd`起動前後での`curl`の結果の違い)を使い、「サーバーとは、あるポート番号でリクエストを待ち受け、応答するプログラムが動いている状態のコンピューター」という定義を自力で構築・説明できるレベルに到達
- 特別なハードウェアではなく「役割・状態」であるという理解も整理

**学習習慣の整備**
- 昨日の内容(サーバーの定義・NAT/PAT・SGの送信元制限・SSH鍵紛失対応など)を翌日の学習開始前に自己テストする「復習チェックリスト」を導入し、習慣化を開始

## ③ 更新後のクラウド構築フロー検証状況

| # | ステップ | 状況 |
|---|---|---|
| ① VPC | △ 概念補足のみ(Dockerに直接対応する機能がないため) |
| ② サブネット | ✅ 実機体験済み(今日) |
| ③ SG | ✅ 実機体験済み(今日、ポート単位のブロック/許可を確認) |
| ④ SSH鍵登録 | △ 一部体験済み(9/10) |
| ⑤ VSI作成 | ✅ 体験済み(9/10) |
| ⑥ パブリックIP | ✅ 体験済み(9/10) |
| ⑦ SSH接続 | ✅ 体験済み(9/10) |
| ⑧ ミドルウェア導入 | ✅ 体験済み(9/10) |

## ④ 明日の学習予定(たたき台)
- IBM Cloud公式ドキュメントで、実際の画面・作成手順を確認し、今日までの体験との対応関係を整理(アカウント不要)
- ④SSH鍵登録を、より実機に近い形で体験できないか検討
- IBM Cloudアカウント作成のブロッカー(クレカ)解消を引き続き並行して試行
- 復習チェックリストの継続(今日の内容も追記)

---

## メモ
- このファイルは9/10の`スライド用サマリー_0910.md`と同じ構成で作成。実際のPowerPoint(Activarch Consultingテンプレート)に転記する際のたたき台として使う想定。
