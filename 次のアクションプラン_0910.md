# 次のアクションプラン(2026/9/10時点)

学習サマリー_0910.mdの内容を踏まえ、直近で実施すべき作業を整理する。

## 前提
- W1(9/8〜9/12)で「ネットワーク基礎」「クラウド基礎概念」「Linux基礎」は自力説明できるレベルに到達済み
- IBM Cloud本体はクレカ承認待ちで停滞中 → 引き続き代替手段(手元環境)で実践を積む方針
- 未着手なのは「VM単位でのインフラ構築(VPC〜SSH接続まで)の一連の流れを手を動かして体験すること」

## タスク1: VirtualBox + Rocky Linux VM構築(最優先)
Dockerコンテナでは体験できなかった要素を埋めるための作業。

1. **環境準備**
   - VirtualBoxのインストール(ホストOSに応じて)
   - Rocky Linux 9 の ISO イメージをダウンロード
2. **VM作成・OSインストール**
   - 新規VM作成(CPU/メモリ/ディスクのスペック割り当てを意識的に決定する)
   - ISOからのインストーラーを実際に操作し、パーティション・ユーザー作成等を一通り経験
3. **ネットワーク設定の比較実験**
   - NAT / ブリッジ / ホストオンリーの3種類を順に試し、それぞれで「ホストからVMに疎通できるか」「VMから外部に疎通できるか」を`ping`や`curl`で確認
   - この違いをIBM CloudのVPC/サブネット/パブリックゲートウェイの概念に対応付けてメモする
4. **SSH鍵の生成・登録・接続**
   - ホスト側で`ssh-keygen`により鍵ペアを作成
   - VM側の`~/.ssh/authorized_keys`に公開鍵を登録
   - `docker exec`を使わず、実際に`ssh`コマンドでVMへ接続する
   - パスワード認証を無効化し、鍵認証のみでの接続に切り替える(本番運用に近い設定を体験)
5. **Webサーバー再構築**
   - 今回はSSH経由でVMにログインした状態から`dnf install -y httpd`を実施
   - `systemctl start httpd` / `systemctl enable httpd`でsystemd経由の起動・自動起動設定を行う(Dockerでは体験できなかった部分)
   - ホストOSのブラウザ/curlからVMのIPにアクセスして疎通確認
6. **振り返りメモ作成**
   - 「VPC→サブネット→SG→VSI→SSH」の各ステップと、今回のVirtualBox作業(ネットワークアダプタ設定・SSH鍵登録・VM起動)の対応関係を表にまとめる

## タスク2: IBM Cloudアカウント作成のブロッカー解消
- クレジットカード審査が下りない原因の切り分け(カード会社への確認、別カードでの再試行、プリペイド/バーチャルカードの利用可否確認)
- 代替として「IBM Cloudの無料枠を使わない学習」を続けつつ、並行してアカウント作成のリトライを継続する

## タスク3: W1の残タスクの棚卸しと座学の補完
- ログ(journalctl等)について座学がまだ未実施 → `journalctl -u httpd`などの基本コマンドを学ぶ
- ネットワーク基礎(ルーティング/NAT/DNS)は「図での整理済み」止まりなので、VirtualBoxのネットワーク実験(タスク1-3)と紐づけて実践的理解に格上げする

## タスク4: W2以降の見通し確認
- W1完了(9/12)後のW2で何を学ぶ予定かをロードマップ上で再確認し、IBM Cloudアカウントが引き続き使えない場合の代替学習方法を事前に用意しておく
