# 学習サマリー(2026/9/10)

## 背景・目的
- Activarch Consultingでの10月アサインに向けた学習ロードマップ実施中
- ゴール:10/1時点で「IBM Cloudでサーバーを立てる業務」の基本用語・作業順序を理解し、プロジェクトのキャッチアップ・推進ができる状態にする
- 現在はW1(9/8火〜9/12金)、フェーズ1「ネットワーク・クラウド基礎概念」の期間

## 前提の制約
- IBM Cloudの無料アカウント作成を試みたが、クレジットカードの承認が下りず作成できず停滞中
- IBM Cloud本体へのアクセスは保留し、代替手段で「クラウドでサーバーを立てる」体験・学習を進めている

---

## 今日実施したこと

### 1. Google Cloud Shell + Docker でLinuxサーバー構築の疑似体験
- IBM Cloudの代替として、クレカ不要のGoogle Cloud Shellを使用
- `docker run -it rockylinux:9 bash` でRed Hat系(Rocky Linux 9)のコンテナを起動
- `dnf install -y httpd` でWebサーバー(Apache)をインストール
- `httpd -D FOREGROUND` で起動を試みたところ、`SIGWINCH`シグナルによりプロセスが終了する事象が発生
  - `cat /etc/httpd/logs/error_log` でログを確認し、原因を特定(フォアグラウンド起動特有の問題)
  - `httpd`(バックグラウンド起動)で再実行し、正常稼働を確認
- `curl http://localhost` でApacheのデフォルトページが返ることを確認
- `/var/www/html/index.html` に独自のHTMLを配置し、正しく配信されることを確認
- **重要な気づき**:この体験はDockerコンテナ上の作業であり、以下の点でクラウドVM構築の本質的な部分(ネットワーク設定、SSH認証、リージョン選択、スペック選択)を体験できていないことが判明
  - `docker exec`でコンテナに直接入っており、SSHを経由していない
  - パブリックIP・ファイアウォール・リージョン等の設定は一切発生していない

### 2. IaaS/PaaS/SaaSの理解
- 当初の理解(ハード/OS/アプリの提供範囲で区分)を、「責任分界点」という軸で精緻化
- 具体例(EC2=IaaS、Heroku/Elastic Beanstalk/Cloud Foundry=PaaS、Gmail/Slack=SaaS)と紐づけ
- 「AWS=IaaS」ではなく「AWSという会社がIaaS/PaaS/SaaS全てを提供している」という誤解を修正
- IBM Cloud VSIがIaaSである理由を、固有名詞に頼らず「OS・ミドルウェアを利用者が用意する必要がある」というロジックで自力説明できる状態まで到達

### 3. リージョン・ゾーンの理解
- 「災害時の可用性確保のための分散」という目的の理解は既に正確
- リージョン(地理的エリア)とゾーン(リージョン内の独立したデータセンター群)の階層関係を補足

### 4. Linux基礎(SSH/権限/プロセス/systemd)の理解
- **SSH**:公開鍵・秘密鍵の非対称暗号の仕組みを深掘り
  - 「公開鍵はみんなで共有する鍵」という誤解を修正し、「秘密鍵と1対1で対応する、個人ごとに存在する鍵」であることを整理
  - 公開鍵はサーバーに登録するものであり、他人に渡すものではない(渡しても実害はないが意味がない)ことを確認
- **権限**:rwx(読む/書く/実行する)×所有者/グループ/その他の3×3構造を整理
  - 今日の作業がrootユーザーで行われていたため、権限の制約を受けていなかったことを確認
  - `sudo`による一時的な権限昇格の考え方にも触れた
- **プロセス**:プログラムの実行中の状態であり、PID・起動者の権限継承・生存状態(Up/Exited)という概念を整理
  - 今日のhttpd起動・SIGWINCHでの終了・再起動の一連の流れを「プロセスのライフサイクル」として振り返り
- **systemd**:プロセス管理の仕組み(役割)であることを理解
  - `systemctl`/`journalctl`はsystemdに指示・確認するためのコマンドであるという関係を整理
  - 今日使ったDockerコンテナにはsystemdが入っておらず、手動起動が必要だった背景も確認

### 5. ログ
- `/etc/httpd/logs/error_log` の確認を通じて実地で体験済み(座学は未実施)

---

## 現状のステータス(W1のやることリストとの対比)

| 項目 | 状況 |
|---|---|
| ネットワーク基礎(ルーティング/NAT/IP・CIDR/DNS) | 図での整理済み、用語の説明は概ね可能 |
| クラウド基礎概念(IaaS/PaaS/SaaS/仮想化/リージョン・ゾーン) | 本日の対話で精度向上、自力説明可能なレベルに到達 |
| Linux基礎復習(SSH/権限/プロセス/systemd/ログ) | 本日の対話と実践で一通り理解、自力説明可能なレベルに到達 |
| IBM Cloud無料アカウント作成・コンソール把握 | クレカ承認待ちで停滞中(未着手) |

---

## 次にやろうとしていること

- **VirtualBoxを使い、Rocky LinuxのVM(仮想マシン)を1から構築する**
- 目的:Dockerコンテナでは体験できなかった以下の要素を、実際に手を動かして体験する
  - OSのISOからのインストール作業
  - ネットワークアダプタの設定(NAT/ブリッジ/ホストオンリー)→ VPC・サブネットの概念の疑似体験
  - SSH鍵の生成・登録・実際のSSH接続(今回は`docker exec`で迂回していた部分)
  - VM単位でのスペック(CPU/メモリ)の割り当て
- これにより、IBM Cloud VSI構築(VPC→サブネット→SG→VSI→SSH)の各ステップの意味を、より実感を持って理解することを狙う
