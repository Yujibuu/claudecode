# 引き継ぎメモ(コンテキストクリア前のまとめ)

## プロジェクトの背景
- Activarch Consultingで10月アサインに向けた学習中
- ゴール: 10/1時点で「IBM Cloudでサーバーを立てる業務」の基本用語・作業順序を理解し、プロジェクトのキャッチアップ・推進ができる状態にする
- W1(9/8火〜9/12金): フェーズ1「ネットワーク・クラウド基礎概念」の期間
- 上長への報告が毎日ある(パワポで進捗共有)

## 前提の制約(継続中)
- IBM Cloud無料アカウント作成がクレカ審査で停滞中(問い合わせメール送信済み、返信待ち)
- ローカルPCの管理者権限がなくVirtualBoxが使えない
- → 代替として**Google Cloud Shell上のDocker**で、IBM Cloudの各概念を疑似体験する方針で進行中

## リポジトリの状態
- リポジトリ: `Yujibuu/claudecode`
- ブランチ: `claude/learning-summary-0910-plan-p3sflo`
- **pushの運用ルール**: ユーザーから明示的に指示があるまでpushしない(ローカルコミットのみ進める)。現在複数コミットがpush待ちの状態。
- リポジトリ内の主なファイル:
  - `学習サマリー_0910.md`(9/10の学習内容、元々あったファイル)
  - `次のアクションプラン_0910.md`
  - `スライド用サマリー_0910.md`(9/10分、上長報告スライドのたたき台)
  - `スライド用サマリー_0911.md`(9/11分、同上)
  - `復習チェックリスト_0911.md`(翌日の自己テスト用、10問)
  - `学習進捗報告_0910.pptx`(9/10の実際のPowerPoint、git管理対象外 `.gitignore`に`*.pptx`追加済み)
  - `サーバー構築フロー図_0911.pptx`(8ステップのアイコン図、pptx、git管理対象外)
  - `.gitignore`(`*.pptx`を除外)

## 学習の進め方(重要な方針)
- **クイズ形式**が非常に効果的だったため、以降もこのスタイルを継続する
- ユーザーは「理解せずコピペで進めるのを避けたい」ため、**コマンドも自分で考えて入力する方針に転換**(直前の会話で開始したばかり)
- **ヒントも一切出さない**でほしいとの明確な要望あり(答えが予測変換に出るので、それ以上のヒントも不要とのこと)
- 詰まったら都度、正確な用語・仕組みまで掘り下げて訂正する(表面的な○×で終わらせない)

## これまでの学習内容(9/10)
- Google Cloud Shell + Dockerで、SSH鍵認証による実接続(`docker exec`ではなく本物のSSH)
- sudo権限昇格の体験(root→wheelグループ付与→パスワード設定→testuserでsudo)
- Webサーバー(httpd)構築、ポート公開で外部疎通確認(Cloud Shell Web Preview使用)
- アクセスログ/エラーログの解読
- NAT/CIDR/DNSの実地確認(`ip route`, `curl ifconfig.me`, `dig`)
- IaaS/PaaS/SaaS・リージョン/ゾーン・Linux基礎の理解(自力説明可能なレベル)

## これまでの学習内容(9/11・本日)
- クラウドで1台サーバーを立てる8ステップを整理: ①VPC ②サブネット ③SG ④SSH鍵登録 ⑤VSI作成 ⑥パブリックIP ⑦SSH接続 ⑧ミドルウェア導入
- Dockerカスタムネットワークで①②③を疑似体験:
  - `docker network create --subnet=...`で2つのサブネット作成(public-subnet 10.0.1.0/24, private-subnet 10.0.2.0/24)
  - 別ネットワーク間はデフォルトで通信不可(VPC/サブネットの隔離を体験)
  - `docker network connect`で明示的に接続すると通信可能に(経路の追加、SGとは別物という訂正あり)
  - 同一ネットワーク内で`iptables`を使い、経路は変えずポート単位でブロック/許可 → これが本来のSGの動き
- 「サーバーとは何か」を上長に問われて答えられなかったことをきっかけに定義を掘り下げ:
  - 最終形: 「サーバーとは、あるポート番号でリクエストを待ち受け、来たリクエストに応答するプログラムが動いている状態のコンピューター」
  - ハードウェアではなく役割・状態であることも整理
- 「理解している」と「実際にIBM Cloudで構築できる」は別物、という認識をすり合わせ済み
- 復習の習慣化(`復習チェックリスト_0911.md`)を開始

## 現在進行中のタスク(コンテキストクリア後、ここから再開)
**「①〜⑧を1つの続き物として、最初から最後まで通しでやってみる」という実践の真っ最中。**

### 進捗状況
1. まず今までのコンテナ・ネットワークをクリーンアップする段階
2. `docker ps -a`実行済み → 4つのコンテナ確認済み(`server-a`, `server-b`, `db-server`, `web-server`、すべてUp状態)
3. `docker network ls`実行済み → `public-subnet`, `private-subnet`の2つのカスタムネットワーク確認済み
4. 稼働中コンテナの削除コマンドを考えてもらっている途中(まだ未実行、ユーザーが自分で考え中)

### Google Cloud Shell環境の現状(実機の状態)
- コンテナ4つ、カスタムネットワーク2つが**まだ残っている**(削除されていない)
- これらを削除してから、8ステップの通し実践に入る予定

### 次にやること
1. コンテナ全削除(`docker rm -f` を使うか等、ユーザーに考えさせる)
2. ネットワーク全削除(`docker network rm`)
3. ①VPC〜⑧ミドルウェア導入までを、1つのコンテナ/ネットワーク構成で一気通貫にやってみる
   - ただし、Dockerには「VPC」概念がないため、①は概念補足のみになる点は前回同様
   - ④SSH鍵登録はまだ「一部体験」評価なので、今回の通し実践できちんと最初から鍵生成→登録まで含めるとより良い

## 進め方の注意点(再開時に厳守)
- コマンドを先に提示せず、「何をしたいか」というゴールだけ伝えて、ユーザー自身に考えさせる
- ヒントも出さない(要望あり)
- 詰まった場合のみ、正確に訂正する
- pushは指示があるまでしない
