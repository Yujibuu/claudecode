# 本日の報告用スライド構成案(2026/9/10)

> 1枚スライドにまとめる際のたたき台。学習が進み次第、この下に追記・更新していく。

---

## タイトル案
「IBM Cloudアサインに向けた学習 進捗報告(9/10)」

## スライド構成(4ブロック案)

### ① 背景・ゴール(1行)
- 10月アサインに向け、IBM Cloudでのサーバー構築業務をキャッチアップ可能なレベルにするための学習中(W1: 9/8〜9/12)
- 前提の制約:IBM Cloud無料アカウントがクレカ審査未通過で作成できず、代替手段で学習継続中

### ② 本日やったこと(実績)

**前半: Docker基礎体験**
- **Google Cloud Shell + Docker**でRocky Linux 9コンテナ上にApacheサーバーを構築し、`curl`で疎通確認まで実施
  - SIGWINCHによるプロセス終了のトラブルシュートも経験(フォアグラウンド起動特有の問題と特定)
- 上記体験の限界に気づき、「SSH経由でのアクセス」「ネットワーク設定」「リージョン/スペック選択」がDocker体験には含まれていないことを整理
- **IaaS/PaaS/SaaS**を「責任分界点」の軸で再整理し、自力説明できるレベルに到達
- **リージョン/ゾーン**の階層関係を補足整理
- **Linux基礎(SSH鍵認証・rwx権限・プロセス・systemd)**を、本日の実機トラブルと紐づけて理解を深化

**後半: 「本物のSSH接続」での実践(VirtualBoxがPC権限問題で使えなかったため、Cloud Shell上で代替実施)**
- ローカルPCの管理者権限がなくVirtualBoxを導入できなかったため、Google Cloud Shell上のDockerコンテナに`openssh-server`を追加し、SSHサーバーとして構成
- ホスト(Cloud Shell)側でSSH鍵ペア(公開鍵/秘密鍵)を作成し、コンテナ側の`authorized_keys`に公開鍵を登録
- `docker exec`(直接侵入)ではなく、**`ssh -i <秘密鍵> -p <ポート> testuser@localhost`で本物のSSH鍵認証接続**に成功
  - 比較実験として、鍵を指定せず接続 → パスワード認証にフォールバック → パスワード未設定のため`Permission denied`となることを確認し、「鍵がないと入れない」ことを実証
- 一般ユーザー(`testuser`)には当初`sudo`権限がないことを確認(`sudo: command not found`)
  - rootアカウントから`sudo`パッケージをインストールし、`testuser`を`wheel`グループに追加(権限昇格の資格付与)、パスワードを設定(本人確認用)
  - `testuser`が自分のパスワードで`sudo dnf install -y httpd`を実行し、権限昇格によるパッケージインストールに成功
- `testuser`権限のまま`sudo /usr/sbin/httpd`でWebサーバーを起動し、`curl http://localhost`でRocky Linuxのテストページが返ることを確認(=サーバーが正常に稼働していることの疎通確認)

### ③ 現状ステータス(W1チェックリスト対比)
| 項目 | 状況 |
|---|---|
| ネットワーク基礎 | 図解済み、実践は未(ポート公開の概念は体験) |
| クラウド基礎概念(IaaS/PaaS/SaaS/リージョン等) | 自力説明可能 |
| Linux基礎(SSH/権限/プロセス/systemd) | 自力説明可能 + 実接続・権限昇格を実機で体験済み |
| IBM Cloudアカウント作成 | クレカ審査待ちで未着手 |

### ④ 次のアクション(明日以降)
- ローカルPCの管理者権限取得(IT部門への依頼)を進め、可能であれば**VirtualBox + Rocky Linux VM構築**でネットワークアダプタ設定(NAT/ブリッジ/ホストオンリー)の比較実験を補完
- 管理者権限が得られない場合は、Cloud Shell上でポート公開(`-p 8080:80`等)を組み合わせ、「外部公開にはSG的なポート許可設定が必要」という概念を引き続き実践で深める
- IBM Cloudアカウント作成のブロッカー(クレカ)解消を並行して試行

---

## メモ:今後の更新方針
- このファイルは学習が進むたびに「②本日やったこと」「③ステータス」「④次のアクション」を追記・更新していく
- 終業時にこの内容を見ながらPowerPoint 1枚(タイトル+4ブロック)に清書する想定
