# 復習チェックリスト(0911分・翌営業日の頭に自己テスト)

> 使い方: 何も見ずに、口に出す or 書き出す形で答えてみる。詰まったら該当項目だけ`学習サマリー`を見て確認し、もう一度言い直す。全部言えたら削除/チェックしてOK。

## 問題リスト

1. **サーバーとは何か?**
   ヒント: 特別なハードウェアの話ではない。ポート番号・待ち受け(listen)・プログラムという言葉を使って説明できるか。

2. **AWS Lambdaのような知らないサービスが出てきたとき、IaaS/PaaS/SaaSのどれか判断する軸は?**
   ヒント: 「責任分界点」。OS/ミドルウェアを利用者が用意する必要があるかどうかで考える。

3. **SSH秘密鍵を紛失したらどうする?**
   ヒント: 「再発行」ではない。新しい鍵ペアを作り、別経路でログインし、登録し直す。

4. **SGで「ポート番号」だけでなく「送信元IP」も絞るのはなぜ?**
   ヒント: 「同じネットワーク内かどうか」ではない。攻撃者が接続を試みる機会そのものを奪うため。

5. **家の複数端末が1つのグローバルIPを共有できるのはなぜ?(NAT/PAT)**
   ヒント: IPアドレスだけでは区別できない。もう1つの数字が対応表に入っている。

6. **VPC・サブネット・VSIの関係を説明できるか?**
   ヒント: 「VPCはサーバーの仮想版」ではない。ネットワークの区画(VPC)→IP範囲での区画分け(サブネット)→そのIPを持つ仮想サーバー(VSI)。

7. **SSH鍵登録とSSH接続、何が違う?**
   ヒント: 準備段階(鍵を登録)と、実際の認証・アクセス(鍵で入る)の違い。

8. **SGがVSI公開前に必要な理由は?**
   ヒント: なければ公開した瞬間に何が起きるか。

9. **(9/11追加) VPC/サブネットの隔離と、SGの許可判定は何が違う?**
   ヒント: 「経路(そもそも繋がる道があるか)」の話と、「経路はあるが特定の通信を通すかどうか」の話は別レイヤー。Dockerの実験で言うと、別ネットワーク間のping不通と、同じネットワーク内でのiptablesブロックの違い。

10. **(9/11追加) `docker network connect`で通信できるようになったのは、SGのルール追加と同じ意味か?**
    ヒント: 違う。あれは経路(ネットワークへの参加)の追加であり、SGの許可判定とは別物。

11. **(9/11追加) SSHの「ホスト鍵」と、ログイン時に使う利用者の鍵(秘密鍵/公開鍵)は何が違う?**
    ヒント: 利用者の鍵は「誰がログインするか」を証明するもの。ホスト鍵は「接続先のサーバーが本物か(なりすましされていないか)」を証明するもの。役割が逆。

12. **(9/11追加) `authorized_keys`に公開鍵を登録するだけでSSHログインできる?**
    ヒント: できない。ファイルの所有者(ログインしたいユーザー自身)とパーミッション(ディレクトリ700・ファイル600など)が緩すぎたり間違っていたりすると、SSHはセキュリティ上の理由でログインを拒否する。

## 習慣化のルール
- 新しい学習を始める前に、まずこのリストを何も見ずに自己テストする(5分程度)
- 答えられたらそのまま次の学習へ。詰まったらその場で`学習サマリー_0910.md`やこれまでの会話を見返して確認
- 新しい日の学習が終わったら、その日出てきた重要な問答をこのファイル形式で追記していく
